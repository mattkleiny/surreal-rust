[package]
name = "surreal-scripting"
description = "A scripting engine for Surreal"
authors.workspace = true
edition.workspace = true

[dependencies]
common = { package = "surreal-common", path = "../../common" }
thiserror = "1.0.26"
mlua = { version = "0.9.4", features = ["vendored", "lua54"] }
